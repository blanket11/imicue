# Imicueへの貢献

コードや文書を変更する人向けの手順です。初回は「ローカルで確認する」と「変更の範囲を決める」を読み、評価や公開に関する節は必要なときに参照してください。

## ローカルで確認する

Node.js 24.14.0、npm 11.9.0を使用します。リポジトリのルートで実行してください。

```sh
npm ci
npx playwright install chromium firefox webkit
npm run check
```

型検査、lint、単体・統合試験、ビルド、配布検査、Rules評価、3ブラウザエンジンのE2Eが順に動きます。実APIは呼びません。E2Eは4173・5184・5185・5193を使用します。このリポジトリのプレビューが起動中なら停止し、他の作業のプロセスは停止しないでください。

開発中のVanillaデモは `npm run dev`、Next.js静的デモはビルド後に `npm run preview:next` で確認できます。具体的な操作は [README](README.md) を参照してください。

## 変更の範囲を決める

[仕様書一覧](docs/README.md)で担当範囲を確認します。Coreは辞書・集計・判定、Browserは同意と観測、ServerはHTTP境界とJev接続を担当します。利用サイトの表示UIはexamplesに置きます。

契約を変える場合は、実装・仕様・対応する受け入れテストを一緒に変更してください。再現用データには架空の製品と合成データを使います。表示時間を読了時間と呼んだり、Rulesのスコアを確率として説明したりしないでください。

コミットの件名・本文は日本語とし、PRには問題、変更後の動作、実行した検証と残る制約を書きます。軽微な説明修正だけなら、新しいテストを増やす必要はありません。疑問や不具合の相談は、このリポジトリのIssueまたは作業中のPRで行えます。

## 公開ファイルに個人の運用情報を入れない

APIキー、アカウント名、キーの管理名、個人の所有ドメイン、契約先、ローカルの絶対パスは公開用の文書・fixture・ログに書かないでください。環境固有の値が必要な説明は、予約済みの例示用ドメインやlocalhost、環境変数名で示します。既存の公開リポジトリへのリンクは維持できます。

コミット前に `git diff --cached` と追加ファイルを確認します。`.gitignore` は文書へ貼った情報を防ぎません。実APIキーの設定・有料試験・公開操作は、通常の開発やテストから分離します。

## 評価結果は回帰確認と意味評価を分ける

`npm run eval:rules` は既存の合成12シナリオを実行し、`test-results/rules-evaluation.json` を出力します。期待値は開発時に作成した回帰基準で、人が妥当性を確認した正解ラベルではありません。developmentは8件、holdoutは4件です。holdoutは比較用に分けたデータですが、既に回帰試験で使っているため、未知データへの性能を証明しません。

意味的な評価では、許容する候補または見送りを人が先にレビューし、その後に同じ入力・候補集合で各方式を比較します。詳細は [公開準備と評価](docs/12-release-preparation.md) を参照してください。期待値に合わせる目的だけで閾値を変えないでください。

## 用語

| 用語 | このプロジェクトでの意味 |
| --- | --- |
| Snapshot | 観測・操作・案内結果を期間内で集計した入力 |
| Decision | 固定候補への推薦、または見送りの結果 |
| fixture | 再現可能な試験用の合成データ |
| 回帰確認 | 変更前後で定義済みの挙動を維持できるかの確認 |
| 意味評価 | 案内内容が場面に合うかを、人の判断と比較する評価 |

更新日: 2026-09-26。
